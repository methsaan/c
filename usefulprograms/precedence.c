#include <stdio.h>

int main(int argc, char *argv) {
	int numOfOp;
	printf("Enter number of operations: ");
	scanf("%d", &numOfOp);
	char op[numOfOp];
	int num[numOfOp+1]
	printf("Enter first number: ");
	scanf("%d", &num[0]);
	for (int x = 0; x < numOfOp; x++){
		printf("Enter next operator: ");
		scanf("%c", &op[x]);
		printf("Enter next number")
	}
	scanf("%d", &num[sizeof(num)/sizeof(*num)-1]);
}
